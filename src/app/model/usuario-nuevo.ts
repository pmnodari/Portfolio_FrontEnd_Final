export class UsuarioNuevo {
    //Atributos del nuevo usuario (!Ignore cuando esta seguro que contendra esas variables)
    nombre!: string;
    nombreUsuario!: string;
    email!: string;
    password!: string;
    authorities!: string[];
}
